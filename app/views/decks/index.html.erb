<%= link_to "New Deck", new_deck_path %>
<% @decks.each do |deck| %>
  <div>
    <%= link_to deck.name, "/decks/#{deck.id}" %>
    <%= link_to "Edit", edit_deck_path(deck) %>
    <%= link_to "Delete" %>
  </div>
<% end %>